{
  "name": "copilot-bridge",
  "version": "0.1.0",
  "description": "Subagent-only MCP bridge that delegates work to target AI runtimes and surfaces one completion event per job through the hook drain.",
  "type": "module",
  "private": true,
  "main": "server.mjs",
  "dependencies": {
    "@github/copilot-sdk": "^1.0.1",
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "engines": {
    "node": ">=22"
  }
}
